package com.indazzlenew.NewCart;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.view.View;

import java.util.List;

/**
 * Created by user on 26-07-2016.
 */
/*
public class CatalogActivity extends AppCompatActivity implements View.OnClickListener {
    List<Product> mProductList;
    ShoppingCartHelper helper;
    Intent productDetailsIntent;

   // mProductList=new ShoppingCartHelper().getCatalog(getResources());

    public List<Product> getmProductList() {
        mProductList = ShoppingCartHelper.getCatalog(getResources());

        @Override
        public void onClick (View view)
     /*  {
       Intent productDetailsIntent;
       productDetailsIntent = new Intent(getBaseContext(),ProductDetailsActivity.class);
        productDetailsIntent.putExtra(ShoppingCartHelper.PRODUCT_INDEX, position);
        startActivity(productDetailsIntent);
  */
    //}

//}

//*/